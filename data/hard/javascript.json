{
  "language": "javascript",
  "difficulty": "hard",
  "total": 100,
  "snippets": [
    { "id": 1, "code": "function fact(n){return n<=1?1:n*fact(n-1)}" },
    { "id": 2, "code": "const fib=n=>n<2?n:fib(n-1)+fib(n-2);" },
    { "id": 3, "code": "const memo=(fn)=>{const c={};return x=>c[x]??(c[x]=fn(x))}" },
    { "id": 4, "code": "const curry=f=>a=>b=>f(a,b);" },
    { "id": 5, "code": "const compose=(f,g)=>x=>f(g(x));" },
    { "id": 6, "code": "function* gen(){yield 1;yield 2;}" },
    { "id": 7, "code": "async function fetchData(){return await Promise.resolve(5)}" },
    { "id": 8, "code": "Promise.all([Promise.resolve(1),Promise.resolve(2)]);" },
    { "id": 9, "code": "const deepCopy=o=>JSON.parse(JSON.stringify(o));" },
    { "id": 10, "code": "const flatten=a=>a.flat(Infinity);" },
    { "id": 11, "code": "const uniq=a=>[...new Set(a)];" },
    { "id": 12, "code": "const groupBy=(a,f)=>a.reduce((r,v)=>((r[f(v)]??=[]).push(v),r),{});" },
    { "id": 13, "code": "const pipe=(...f)=>x=>f.reduce((v,f)=>f(v),x);" },
    { "id": 14, "code": "const delay=ms=>new Promise(r=>setTimeout(r,ms));" },
    { "id": 15, "code": "const once=f=>{let d=false;return(...a)=>d?null:(d=true,f(...a))}" },
    { "id": 16, "code": "const debounce=(f,t)=>{let id;return(...a)=>{clearTimeout(id);id=setTimeout(()=>f(...a),t)}}" },
    { "id": 17, "code": "const throttle=(f,t)=>{let p=0;return(...a)=>{let n=Date.now();if(n-p>t){p=n;f(...a)}}}" },
    { "id": 18, "code": "const perm=s=>s.length? [...s].flatMap((c,i)=>perm(s.slice(0,i)+s.slice(i+1)).map(p=>c+p)):[\"\"]" },
    { "id": 19, "code": "const gcd=(a,b)=>b?gcd(b,a%b):a;" },
    { "id": 20, "code": "const lcm=(a,b)=>a*b/gcd(a,b);" },
    { "id": 21, "code": "const binary=n=>n.toString(2);" },
    { "id": 22, "code": "const rev=s=>[...s].reverse().join('');" },
    { "id": 23, "code": "const pal=s=>s===rev(s);" },
    { "id": 24, "code": "const primes=n=>[...Array(n).keys()].filter(x=>x>1&&[...Array(x).keys()].slice(2).every(i=>x%i));" },
    { "id": 25, "code": "const chunk=(a,s)=>Array.from({length:Math.ceil(a.length/s)},(_,i)=>a.slice(i*s,i*s+s));" },
    { "id": 26, "code": "const range=n=>[...Array(n).keys()];" },
    { "id": 27, "code": "const sum=a=>a.reduce((x,y)=>x+y,0);" },
    { "id": 28, "code": "const avg=a=>sum(a)/a.length;" },
    { "id": 29, "code": "const max=a=>Math.max(...a);" },
    { "id": 30, "code": "const min=a=>Math.min(...a);" },
    { "id": 31, "code": "const shuffle=a=>a.sort(()=>Math.random()-0.5);" },
    { "id": 32, "code": "const sleep=t=>new Promise(r=>setTimeout(r,t));" },
    { "id": 33, "code": "const deepFreeze=o=>Object.freeze(o);" },
    { "id": 34, "code": "const flattenObj=(o,p='')=>Object.entries(o).reduce((r,[k,v])=>Object.assign(r,typeof v==='object'?flattenObj(v,p+k+'.'):{[p+k]:v}),{});" },
    { "id": 35, "code": "const mapObj=(o,f)=>Object.fromEntries(Object.entries(o).map(([k,v])=>[k,f(v)]));" },
    { "id": 36, "code": "const zip=(a,b)=>a.map((v,i)=>[v,b[i]]);" },
    { "id": 37, "code": "const unzip=a=>[a.map(x=>x[0]),a.map(x=>x[1])];" },
    { "id": 38, "code": "const intersect=(a,b)=>a.filter(x=>b.includes(x));" },
    { "id": 39, "code": "const diff=(a,b)=>a.filter(x=>!b.includes(x));" },
    { "id": 40, "code": "const union=(a,b)=>uniq([...a,...b]);" }
  ]
}